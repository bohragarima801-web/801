const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const admins = await prisma.user.findMany({ where: { role: { slug: { not: 'devotee' } } }, include: { role: true } }); for (const admin of admins) { console.log('Deleting', admin.email); await prisma.user.delete({ where: { id: admin.id } }); } console.log('Done'); } main();
