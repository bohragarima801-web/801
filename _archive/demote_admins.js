const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const roles = await prisma.role.findMany(); const devoteeRole = roles.find(r => r.slug === 'devotee'); if (!devoteeRole) return console.log('No devotee role'); const result = await prisma.user.updateMany({ where: { role: { slug: { not: 'devotee' } }, email: { not: 'Diksha@gmail.com' } }, data: { roleId: devoteeRole.id } }); console.log('Demoted', result.count, 'users to devotee'); } main();
